x-healthcheck: &healthcheck
  test: ["CMD-SHELL", "wget -qO- http://localhost:8080/ping | grep -q '^OK$' || exit 1"]
  interval: 30s
  timeout: 30s
  retries: 3
  start_period: 15s

services:
  traefik:
    image: traefik:3.6.8
    container_name: traefik
    restart: on-failure
    environment:
      - CF_DNS_API_TOKEN=${CF_DNS_API_TOKEN}
      - BASE_DOMAIN=${BASE_DOMAIN}
    healthcheck: *healthcheck
    command:
      - --certificatesresolvers.cf.acme.caserver=https://acme-v02.api.letsencrypt.org/directory
      - --certificatesresolvers.cf.acme.dnsChallenge.delayBeforeCheck=0
      - --certificatesresolvers.cf.acme.dnsChallenge.provider=cloudflare
      - --certificatesresolvers.cf.acme.dnsChallenge.resolvers=1.1.1.1:53
      - --certificatesresolvers.cf.acme.storage=/certs/acme.json
      - --entryPoints.web.address=:80
      - --entryPoints.web.http.redirections.entryPoint.scheme=https
      - --entryPoints.web.http.redirections.entryPoint.to=websecure
      - --entryPoints.websecure.address=:443
      - --entrypoints.websecure.http.tls=true
      - --entrypoints.websecure.http.tls.certResolver=cf
      - --entrypoints.websecure.http.tls.domains[0].main=*.${BASE_DOMAIN}
      - --providers.docker=true
      - --providers.docker.exposedbydefault=true
      - --providers.docker.defaultRule=Host(`{{ .Name }}.${BASE_DOMAIN}`)
      - --providers.docker.watch=true
      - --providers.docker.network=network_internal
      # Default middleware for all services
      - --entryPoints.web.http.middlewares=error-pages-middleware
      - --entryPoints.websecure.http.middlewares=error-pages-middleware
      # Default TLS for websecure
      - --entrypoints.websecure.http.tls.certResolver=cf
      - "--log.level=DEBUG"
      - "--accesslog=true"
      - "--api.insecure=true"
      - --ping
    networks:
      - network_internal
      - internal
    ports:
      - 80:80
      - 443:443
      - 443:443/udp
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - /home/xstar97/docker/data/traefik/certs:/certs
    labels:
      traefik.enable: true
      # traefik.http.services.traefik.loadbalancer.server.port: 8080
      homepage.group: Networking
      homepage.name: Traefik
      homepage.icon: traefik.png
      homepage.href: ${HP_REF}
      homepage.widget.type: traefik
      homepage.widget.url: ${HP_WIDGET_URL}
      homepage.description: Traefik dashboard
      wud.tag.include: ^\d+\.\d+\.\d+$
    depends_on:
      error-pages: {condition: service_healthy}
  error-pages:
    container_name: error-pages
    image: ghcr.io/tarampampam/error-pages:3.8.0 # using the latest tag is highly discouraged
    networks:
      - internal
    environment:
      TEMPLATE_NAME: app-down # set the error pages template
    labels:
      traefik.enable: true
      # Fallback router for 404/5xx errors - must be on web entrypoint for catch-all
      traefik.http.routers.error-pages-router.rule: HostRegexp(`.+`)
      traefik.http.routers.error-pages-router.priority: 1
      traefik.http.routers.error-pages-router.entrypoints: web,websecure
      # Error middleware definition
      traefik.http.middlewares.error-pages-middleware.errors.status: 400-599
      traefik.http.middlewares.error-pages-middleware.errors.service: error-pages-service
      traefik.http.middlewares.error-pages-middleware.errors.query: /{status}.html
      # Service config
      traefik.http.services.error-pages-service.loadbalancer.server.port: 8080
      wud.tag.include: ^\d+\.\d+\.\d+$
networks:
  network_internal:
    external: true
  internal:
    driver: bridge
